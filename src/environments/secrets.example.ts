/**
 * Copia este archivo como secrets.ts y rellena los valores.
 * secrets.ts está en .gitignore y NO se sube al repositorio.
 */
export const secrets = {
  prestashop: {
    apiKey: '',  // API key de Prestashop (solo lectura)
    baseUrl: '', // URL base de la tienda Prestashop (sin / final)
  }
};
