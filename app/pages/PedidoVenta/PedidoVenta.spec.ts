/*
import { PedidoVenta } from './PedidoVenta';
import { PedidoVentaService } from './PedidoVenta.service';

let servicio: PedidoVentaService = new PedidoVentaService();
let pedidoVenta: PedidoVenta = new PedidoVenta(servicio);

export function main(): void {
    'use strict';

    describe('PlantillaVenta', () => {

        it('se ha tenido que crear correctamente la plantilla', () => {
            expect(pedidoVenta).not.toBeNull();
        });

        it('no se puede pasar a seleccionar productos sin haber seleccionado un cliente antes', () => {
            expect(false).toBeTruthy();
        });
    });
}
*/
